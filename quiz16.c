#include <stdio.h>

int factorial(int x){
    if(x == 0 || x == 1) return 1;
    else return x * factorial(x - 1);
}

int ways(int m, int n){
    return 
}

int main(){
    int t = 0;
    int m[10] = {0}, n[10] = {0};
    scanf("%d", &t);
    for(int i = 0; i < t; i++) scanf("%d %d", &m[i], &n[i]);
}