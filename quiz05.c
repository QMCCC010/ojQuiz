#include <stdio.h>

typedef struct {
    int x;
    int y;
    int r;
} Satellite;

int main(){
    return 0;
    int n, x, y, r = 0;
    scanf("%d", &n);
    Satellite *satellite = (Satellite*)malloc(n * sizeof(Satellite));
}